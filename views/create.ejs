<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link href="https://fonts.googleapis.com/css?family=Megrim|Nunito+Sans:400,900" rel="stylesheet" />
    <link rel="icon"
        href="https://emojipedia-us.s3.dualstack.us-west-1.amazonaws.com/thumbs/240/apple/155/ear-of-maize_1f33d.png" />

    <title>RIKKEI FARM</title>
    <link rel="stylesheet" href="create.css">
   
</head>  

<body>
    <div class="container">
        <h1 id="title">🥦Add more fruit🌽</h1>
        <figure class="card">
            <form method="POST" action="" class="create-form" method="post">
                <div class="input-wrapper">
                    <input class="input" type="text" name="productName" placeholder="Product Name">
                </div>
                <div class="input-wrapper">
                    <input class="input" type="text" name="image" placeholder="Image">
                </div>
                <div class="input-wrapper">
                    <input class="input" type="text" name="from" placeholder="Country">
                </div>
                <div class="input-wrapper">
                    <input class="input" type="text" name="nutrients" placeholder="Nutrients">
                </div>
                <div class="input-wrapper">
                    <input class="input" type="number" name="quantity" placeholder="Quantity">
                </div>
                <div class="input-wrapper">
                    <input class="input" type="text" name="price" placeholder="Price">
                </div>
                <div class="input-wrapper">
                    <input class="input" type="text" name="description" placeholder="Description">
                </div>
                <div class="input-wrapper checkbox">
                    <label for="">
                        Organic
                    </label>
                    <input class="tick" name="organic" type="checkbox" checked="true" />
                </div>
                <button>
                    🥭🥭BOOM! Add fruit🥦🥦
                </button>
            </form>
        </figure>
    </div>

    <script>
        let form = document.getElementsByClassName("create-form")[0]
        form.addEventListener("submit", function (e) {
            e.preventDefault();
            let productName = form.productName.value
            let image = form.image.value
            let from = form.from.value
            let nutrients = form.nutrients.value
            let quantity = form.quantity.value
            let price = form.price.value
            let description = form.description.value
            let organic = form.organic.checked
            if (productName === "" || image === "" || from === "" || nutrients === "" || quantity === "" || price === "" || description === "") {
                document.getElementById("title").innerHTML = "🥦Invalid Input🌽"
            }
            else {
                document.getElementById("title").innerHTML = "🥦Add more fruit🌽"
                form.submit()
            }
        })
    </script>
</body>

</html>